<!--
`Closes #N` is not decoration. It is the only thing that populates the Linked pull requests
field on the issue's row of the Arkive Roadmap board: that field is issue-side, so it reads the
closing keyword from THIS description. A keyword in a commit message does not count, and neither
does a bare `#N` mention.

Layer and App need nothing from you here -- `project-sync.yml` derives both from the diff.
-->

Closes #

## What changed

<!-- The outcome, and why this shape rather than the obvious alternative. -->

## Verification

<!--
What you actually ran, and its result. Not "tests pass" -- which suite, and what it reported.
If something is unverified or left open, say so here rather than leaving it to be discovered.
-->
