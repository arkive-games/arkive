// Keep map-engine chrome on the shared Arkive default. Game-owned marker
// artwork and data colors remain independent of this shared theme object.
import { DEFAULT_MAP_THEME } from "@gamemap/map-engine-gl/theme";

export const aionTheme = DEFAULT_MAP_THEME;
