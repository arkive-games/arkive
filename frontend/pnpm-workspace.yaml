packages:
  - "packages/*"
  - "apps/*"

# pnpm 11 no longer reads settings from the "pnpm" field of package.json
# (pnpm/pnpm#10086), so the rolldown-vite swap has to live here.
overrides:
  vite: "npm:rolldown-vite@7.2.2"
